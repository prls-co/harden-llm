apiVersion: 1

deleteDatasources:
  - name: Prometheus
    orgId: 1
  - name: Loki
    orgId: 1
  - name: Tempo
    orgId: 1
  - name: PRLS Loki prod
    orgId: 1
  - name: PRLS Loki nonprod
    orgId: 1
  - name: PRLS Loki test
    orgId: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: harden-prometheus
    orgId: 1
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      httpMethod: POST
      exemplarTraceIdDestinations:
        - name: trace_id
          datasourceUid: harden-tempo
  - name: Loki
    type: loki
    uid: harden-loki
    orgId: 1
    access: proxy
    url: http://loki:3100
    editable: false
    jsonData:
      httpHeaderName1: X-Scope-OrgID
      maxLines: 1000
      derivedFields:
        - name: TraceID
          matcherRegex: '"?trace_id"?[=:]\s*"?([0-9a-f]{32})"?'
          datasourceUid: harden-tempo
          url: '$${__value.raw}'
          urlDisplayLabel: View trace
    secureJsonData:
      httpHeaderValue1: fake
  - name: Tempo
    type: tempo
    uid: harden-tempo
    orgId: 1
    access: proxy
    url: http://tempo:3200
    editable: false
    jsonData:
      tracesToLogsV2:
        datasourceUid: harden-loki
        spanStartTimeShift: -5m
        spanEndTimeShift: 5m
        tags:
          - key: service.name
            value: service_name
        filterByTraceID: true
        filterBySpanID: true
        customQuery: false
      tracesToMetrics:
        datasourceUid: harden-prometheus
        spanStartTimeShift: -5m
        spanEndTimeShift: 5m
        tags:
          - key: gen_ai.provider.name
            value: provider
          - key: gen_ai.operation.name
            value: call_type
        queries:
          - name: Provider request rate
            query: 'sum(rate(harden_llm_provider_attempts{$$__tags}[5m]))'
      serviceMap:
        datasourceUid: harden-prometheus
      nodeGraph:
        enabled: true
      search:
        hide: false
      traceQuery:
        timeShiftEnabled: true
        spanStartTimeShift: -5m
        spanEndTimeShift: 5m
      streamingEnabled:
        search: true
        metrics: true
  - name: PRLS Loki prod
    type: loki
    uid: prls-loki-prod
    orgId: 1
    access: proxy
    url: http://loki:3100
    editable: false
    jsonData:
      httpHeaderName1: X-Scope-OrgID
      maxLines: 1000
      derivedFields:
        - name: TraceID
          matcherRegex: '"?trace_id"?[=:]\s*"?([0-9a-f]{32})"?'
          url: '$${__value.raw}'
          urlDisplayLabel: Copy trace ID
    secureJsonData:
      httpHeaderValue1: prod
  - name: PRLS Loki nonprod
    type: loki
    uid: prls-loki-nonprod
    orgId: 1
    access: proxy
    url: http://loki:3100
    editable: false
    jsonData:
      httpHeaderName1: X-Scope-OrgID
      maxLines: 1000
      derivedFields:
        - name: TraceID
          matcherRegex: '"?trace_id"?[=:]\s*"?([0-9a-f]{32})"?'
          url: '$${__value.raw}'
          urlDisplayLabel: Copy trace ID
    secureJsonData:
      httpHeaderValue1: nonprod
  - name: PRLS Loki test
    type: loki
    uid: prls-loki-test
    orgId: 1
    access: proxy
    url: http://loki:3100
    editable: false
    jsonData:
      httpHeaderName1: X-Scope-OrgID
      maxLines: 1000
      derivedFields:
        - name: TraceID
          matcherRegex: '"?trace_id"?[=:]\s*"?([0-9a-f]{32})"?'
          url: '$${__value.raw}'
          urlDisplayLabel: Copy trace ID
    secureJsonData:
      httpHeaderValue1: test
